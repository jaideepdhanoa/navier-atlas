# Builders

This package is intentionally portable. It does not depend on Tasklet.

Run from the repository root with:

```bash
cd deck-studio
PYTHONPATH=builders python -m deck_studio validate --root .
```

For convenience, install in editable mode or set `PYTHONPATH=builders`.
