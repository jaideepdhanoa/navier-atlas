# Render spec — Growth Case on the partner front end (for Claude)

**Status:** ✅ MERGED TO GOLD (Grab, Gold #4 export `navier-export-20260607T034828Z.zip`, 2026-06-07).
Content lane only — geometry byte-identical to Gold #3 (5154 routes). Block lives in each partner
proposal as `growth_case`. Generated by `finance/model/growth_frontend_block.py` from the partner's
growth-case + rollup JSON. For Grab it now ships in `data-clean/partners/grab.json.growth_case`.

## What ships (3 render-ready sub-blocks)

### 1. `revenue_potential` — the floor-and-prize ladder
- Render as a **vertical ladder / stacked rungs**, floor at bottom, prize at top.
- Each rung: `label`, `whose_money` chip, `basis` (one line), banded `display.{low,mid,high}`, `confidence` tag.
- **Headline the MID.** Show low–high as a faint range. Never lead with `high`.
- Show the `whose_money_legend` + `anchor_note` (the one grounded number everything traces to).
- Color the "whose money" axis: Navier transport rev vs total journey wallet vs **partner's own P&L** (the rung that matters most to the partner).

### 2. `phase_economics` — 3 horizons (Prove / Scale / Mature)
- Render as a **3-column table or stepped cards**, left→right = time.
- Per horizon: `name`, `horizon`, `scope`, `capture`, `vessel`, fleet (point or band), `navier_transport_rev_display`, `partner_platform_rev_display`, `co2_saved_t_yr`, `confidence`.
- Prove = grounded/solid. Mature = banded/softer treatment (med-low).
- This is the "revenue potential for phases" view Jaideep asked for.

### 3. `vessel_sizing` — right hull per leg
- Render as a **card row**: N30 Pioneer II (solid), N35 Shuttle (solid, "2027"), Quanta-LR (**amber-dashed**, "H2 2026+").
- Each card: `pax`, `range_nm`, `status`, `role`. Show the `range_gate_note`.
- Quanta-LR MUST render amber-dashed (roadmap), consistent with atlas arc convention.

## Confidence / honesty rules (hard)
- Lead with the **floor** and the **corridor COUNT** (ID-traceable: 341 greenfield + 35 sourced = 376 mapped Pioneer-range corridors in Grab geography). Counts are harder evidence than the greenfield $ factor.
- Every dollar rung above SOM-floor is **banded**; MID is the headline; high is ceiling only.
- `_render_chip_flag.needs_new_layouts` = `revenue_ladder`, `phase_economics_table`, `vessel_sizing_cards`.

## Reconciliation DONE (2026-06-07 — applied to gold)
Stale narrative numbers reconciled to the model:
- `network_thesis.stats` "At scale" — `250+ vessels` → **`800+ vessels`**; `on 120+ booked corridors` → **`375+ mapped corridors (full-network floor)`**.
- `end_state.steady_state.total_corridors` `120+ booked` → **`375+ mapped water corridors`**.
- `end_state.steady_state.vessels_at_scale` `250+` → **`800+ at full-network floor, scaling to 2,700+ at maturity`**.
- `steady_state_ceiling` `2678` → **`3638`** (Mature Pioneer-equivalent mid).
- **Left as-is (flagged):** `committed_fleet: 1071` (derivation predates model; not contradicted by the ladder — do NOT free-float on front end without a source). Geographic `phases[].boats` (8/177/442/883) left untouched — that is the WHERE axis, distinct from the economic horizons.

## GATE — RESOLVED ✅ (Jaideep, 2026-06-07)
- **$ partner-opportunity-sizing is CLEARED for external use.** The "$100M+" ban applies only to *Navier's own raise / self-finance* framing, NOT to the partner's prize. The full ladder ($208M / $934M / $11.2B / $505M) ships. Mechanical leak gate (`partition_spec.scan_text`) does not pattern dollar figures and returns 0 hits on the block.
