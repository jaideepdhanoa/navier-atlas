# Changelog — Gold #12 (Jaideep decisions batch, 2026-06-07)

Append-only zip-patch on Gold #11. Five approved decisions executed + new-market geometry.

## 1. New-market geometry (+11 cities, +40 BP POIs, +25 routes)
Via `add_market.py` (LB-25). Sourced web-verified boarding points; built with the
build.py stages (BP-POI inject → ICR water-validated spokes → route_labels → scrub).
- **Okinawa / Yaeyama** (yaeyama-japan 6 BPs / 11 routes; okinawa-main-japan 4 BPs / 9 routes) — D5
- **Shanghai** (shanghai-china) — **NEW map pin added** (city_anchor Shiliupu Wharf) + 4 BP POIs.
  Huangpu river-crossings scrubbed by the sea-grid land mask (too narrow) → 0 routes; the
  **map node is the D5 deliverable.** — D5
- **Galápagos** (santa-cruz 3, san-cristóbal 1, isabela 1 BPs) — 0 surviving routes (inter-island
  heroes are cross-city, not generated by intra-cluster spokes)
- **Kenya** (mombasa 5, lamu 4 BPs) — 1 route
- **Belize** (belize-city-cayes 4, ambergris-caye 4, placencia 4 BPs) — 4 routes
- Totals now: **11,337 POIs / 5,167 routes / 161 city pins.** Strict superset of #11.
- SEAL FEATURES_BY_TYPE + ROUTES blobs re-hashed; other blobs byte-identical.

## 2. boats := Vessels KPI (Jaideep decision — consistency, KPI-anchored)
31 phases across 9 partners where `phase.boats` ≠ the phase "Vessels"/"Fleet" KPI →
`boats` overwritten with the parsed KPI integer. Applied to BOTH `data-clean/partners/`
(shipped) and `partner-pitch/partners/` (source). 0 disagreements remain.

## 3. P0.1B re-files (8 applied)
Deterministic, ID-based: a featured route moves only when its endpoint node-ids fall
outside its current phase.cities AND are fully contained by another phase. 8 such routes
re-homed (abu-dhabi-itc, careem, dubai-rta×2, hawaii, red-sea-global×2, singapore-mpa).
The remainder of the original 19-item queue was already handled by Gold #9 or is
coarse-phase (front-end masks). Applied to both surfaces.

## 4. Brunei ×3 orphan briefs removed (D4)
Removed `bandar-seri-begawan-brunei`, `muara-brunei-bay-brunei`, `temburong-bangar-brunei`
(verified: no pin, no POIs, no routes). `brunei-darussalam` cluster brief retained.
city_briefs 167 → 164.

## 5. Cluster briefs sealed (NEW SURFACE — D3)
`data-clean/cluster_briefs/` added (32 files). Tier set by **exact route_id resolution**:
first-class iff ≥1 signature_route.route_id resolves in gold ROUTES.
- **29 first-class**, **3 tag-only** (belize, galapagos-ecuador, kenya — signature hero
  corridors not yet built; map pins/spokes exist but named heroes don't).
- **RENDER INTEGRATION REQUIRED (Claude side):** the website build must bake
  `cluster_briefs/` like it bakes `city_briefs/` and surface a `tier` field.
- Source: `partner-pitch/cluster_briefs/` (mirrored).

## Gates
bp_on_water PASS (11 new clusters, all coords web-verified, land-crossers auto-scrubbed) ·
superset PASS · leak-scan PASS (0) · degenerate-label 0 · pioneer-over-range 0.
