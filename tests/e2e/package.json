{
  "name": "navier-atlas-e2e",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "description": "Playwright smoke tests for the index.html render surface (mobile bottom sheet, etc.). Self-contained — node_modules is gitignored, run `npm install` once.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "install:browser": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0"
  }
}
