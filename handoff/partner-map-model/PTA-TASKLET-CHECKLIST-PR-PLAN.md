# PTA Tasklet checklist — PR plan

**From:** Grok · **Date:** 2026-07-02 · **Status:** ready for Tasklet intake  
**Gold reference:** `bahrain-motc` · **Convention:** `PTA-ECONOMICS-CONVENTION.md`  
**Fleet scope:** 24 authorities in PTA Batch 5 (sealed) + 6 `public_transit` partners outside the lane

Grok has shipped: route seals, `regen_pta_economics.py` for all 24, and the PTA public-value renderer (`_ptaEconomicsHtml` in `index.html`, live at `a0ab6dfc`). **Tasklet owns everything below.**

---

## PR stack (recommended order)

| PR | Title | Partners | Blocks |
|----|-------|----------|--------|
| **PR-1** | PTA economics presentation scrub (batch 5) | 24 | — |
| **PR-2** | PTA fidelity TRIM — Mumbai | `mumbai-mmb` | — |
| **PR-3** | PTA fidelity TRIM — Brisbane + Hamburg | `brisbane-citycat`, `hamburg-hadag` | — |
| **PR-4** | PTA fidelity TRIM — NYC + SF Bay + TfNSW + Boston + Stockholm | 5 partners | — |
| **PR-5** | PTA P2 narrative copy pass | 8 named | PR-1 helpful |
| **PR-6** | PTA Batch 6 — dossier + rewrite (BC Ferries + Hawaiʻi) | `bc-ferries`, `hawaii` | Grok seal PRs follow |
| **PR-7** | PTA Batch 6 — dossier + rewrite (WSF + remaining 3) | `wsf`, `fullers360`, `maldives-government`, `norway-fjords`, `shun-tak` | Grok seal PRs follow |

Run fidelity audit after each TRIM PR:
```bash
python3 scripts/audit_proposal_fidelity.py --partner <slug>
node scripts/audit-partner-route-linkage.mjs --partner <slug>
```

---

## Shared file paths (every partner)

| Surface | Path | Notes |
|---------|------|-------|
| **Public ship JSON** | `data-clean/partners/<slug>.json` | Baked into `atlas-data.js` — **this is what ships** |
| **Authoring mirror** | `partner-pitch/partners/<slug>.json` | Keep in sync with `data-clean/` on every edit |
| **Dossier** (batch 5 + new) | `handoff/partner-map-model/PTA-DOSSIER-<slug>.json` | Source of truth for BPs, pairs, hazards |
| **Grok routing spec** | `handoff/partner-map-model/GROK-SPEC-<slug>-domestic-routing-*.md` | Tasklet authors; Grok executes seal |
| **Fidelity receipt** | `handoff/partner-map-model/PROPOSAL-FIDELITY-<slug>.md` | Regenerated by audit script |
| **Seal receipt** (post-Grok) | `handoff/partner-map-model/PTA-SEAL-RECEIPT-<slug>.json` | Grok-owned; Tasklet does not edit |

**Do not touch:** sealed `route_id` geometry, `scripts/pta/regen_pta_economics.py` quantified metrics (Grok regen lane), `index.html` renderer.

---

## PR-1 — Economics presentation scrub (all 24 batch-5 authorities)

Apply to **both** `data-clean/partners/<slug>.json` and `partner-pitch/partners/<slug>.json`.

### Fields to edit (`growth_case` block)

| Field | Action |
|-------|--------|
| `growth_case._render_chip_flag` | **Delete** entire object |
| `growth_case._marine_tam_split_provenance` | **Delete** entire object (7 partners — see table below) |
| `growth_case.revenue_potential.headline` | Replace commercial framing. Suggested: *"Indicative operating revenue as the network matures — public value leads."* |
| `growth_case.revenue_potential.cite_rule` | **Delete** if present |
| `growth_case.revenue_potential.rungs[].whose_money` | Keep in JSON for provenance but verify no `water_transport_market` rung remains |
| `growth_case.phase_economics.headline` | Replace *"Operating revenue by phase"* with *"Network maturity — public value and indicative fare revenue"* |
| `growth_case.modal_headline` | Set to `"What it returns to the public"` (most already have this) |
| `growth_case.modal_lead` | Authority-specific; must not mention opportunity/prize/wallet/GMV/TAM |
| `growth_case.public_value.levers[]` | Replace with full `interpretation_for_navier` + `policy_targets.relevance` from dossier (no truncation) |
| `growth_case.public_value.operating_model[]` | Replace generic placeholders with authority-specific fare system names (see per-partner table) |

### Fields to **not** delete (Grok regen owns numbers)

- `growth_case.public_value.metrics[]` — quantified CO₂, trips, minutes, corridors
- `growth_case._provenance` — seal corridor count, regen timestamp
- `growth_case.ladder_transitions[]` — plain headline/basis only; drop any `derivation` / `multipliers_cited` if present

### Hard rules (`PTA-ECONOMICS-CONVENTION.md`)

- No partner-facing SOM / SAM / TAM / GMV / journey wallet / platform take / super-app language
- No `water_transport_market` rung
- Fares and cost-recovery: *"set with the authority"* — never fabricate subsidy per passenger

### Per-partner scrub checklist (batch 5)

| `partner_id` | Display | `_marine_tam_split` | Operating-model specifics to add |
|--------------|---------|---------------------|----------------------------------|
| `abu-dhabi-itc` | Abu Dhabi ITC | — | Integrate with existing water-taxi / island-ferry ticketing |
| `auckland-ferries` | Auckland Ferries | — | AT HOP fare integration |
| `bahrain-motc` | Bahrain MOTC | **drop** | Masar app; Vision 2030 / net-zero 2060 tie-in in levers |
| `bangkok-chao-phraya` | Bangkok Chao Phraya | — | BTS Sathorn interchange; MINE Smart Ferry precedent |
| `boston-mbta-ferry` | Boston MBTA Ferry | — | CharlieCard / MBTA fare system |
| `brisbane-citycat` | Brisbane CityCat | — | go card / TransLink |
| `dubai-rta` | Dubai RTA | — | Nol / RTA multimodal |
| `hamburg-hadag` | Hamburg HADAG | — | HVV / HADAG tariff |
| `hong-kong` | Hong Kong | — | Octopus / existing ferry operators |
| `istanbul-sehir-hatlari` | İstanbul Şehir Hatları | — | Istanbulkart |
| `kochi-water-metro` | Kochi Water Metro | — | Kochi One card |
| `lisbon-transtejo` | Lisbon Transtejo | — | Navegante |
| `mumbai-mmb` | Mumbai MMB | — | BEST / water-transport tariff (set with MMB) |
| `nyc-ferry` | NYC Ferry | **drop** | NYC Ferry flat fare / OMNY |
| `qatar` | Qatar MOT | **drop** | Sila integration; 2030 emissions target |
| `rakta` | RAKTA | — | RAK public transport (set with authority) |
| `sf-bay-ferry` | SF Bay Ferry | — | Clipper Card |
| `singapore-mpa` | Singapore MPA | **drop** | Existing harbourcraft / MPA licensing context |
| `stockholm-waxholm` | Stockholm Waxholm | — | SL / Waxholmsbolaget |
| `thames-clippers` | Thames Clippers | **drop** | Uber Boat ticketing / TfL interchange |
| `transport-nsw` | Transport for NSW | **drop** | Opal / Sydney Ferries network |
| `vancouver-seabus` | Vancouver SeaBus | — | TransLink Compass |
| `venice-actv` | Venice ACTV | — | ACTV / Venezia Unica |
| `wsf` | Washington State Ferries | **drop** | WSF fare classes / ORCA (where applicable) |

### PR-1 acceptance

```bash
# No internal layout-debt keys remain
rg '_render_chip_flag|_marine_tam_split_provenance|cite_rule|water_transport_market' data-clean/partners/{abu-dhabi-itc,auckland-ferries,...,wsf}.json
# → 0 hits

# Rebuild + spot-check intro step 2 on 2–3 authorities
BUILD_PROFILE=public node scripts/build.mjs --profile=public
BUILD_PROFILE=public node scripts/build-site.mjs --profile=public
```

---

## PR-2 — Fidelity TRIM: `mumbai-mmb`

**Verdict:** TRIM (9 TRIM + 2 DROP) · Receipt: `PROPOSAL-FIDELITY-mumbai-mmb.md`

### Files

- `data-clean/partners/mumbai-mmb.json`
- `partner-pitch/partners/mumbai-mmb.json`

### Fields to fix

| Surface | Route ID | Action |
|---------|----------|--------|
| `journeys_unlocked[]` | `rn-c9bcc9219b04`, `rn-af6a20ee2a0a`, `rn-a685bc50d3c2`, `rn-0c05727c37fa` | Set `distance_nm` to match sealed route geometry (cards currently 2.1–2.5 nm vs routes 6–13 nm) |
| `phases[].featured_routes[]` | same + `rn-c70751e14751` | Sync `distance_nm`; rewrite card copy if needed |
| `phases[].featured_routes[]` | `ics-ed747a4789` | **DROP** — BP binding: Belapur ↔ Nerul labels ≠ route endpoints |
| `phases[].featured_routes[]` | `ics-3964e5583e` | **DROP** — BP binding: Gateway ↔ Rewas labels ≠ route endpoints |

### Acceptance

```bash
python3 scripts/audit_proposal_fidelity.py --partner mumbai-mmb
# → PASS, journey_bp=0, trim=0
```

---

## PR-3 — Fidelity TRIM: `brisbane-citycat` + `hamburg-hadag`

### `brisbane-citycat` (10 TRIM items)

**Receipt:** `PROPOSAL-FIDELITY-brisbane-citycat.md`

Fix `distance_nm` on journeys + featured routes:

| Route ID | Card vs route delta |
|----------|---------------------|
| `rn-771a3b2ef251` | 2.6 nm vs 1.6 nm |
| `rn-e3914af94b36` | 1.0 nm vs 0.6 nm |
| `rn-19fbaad122e8` | 6.5 nm vs 3.7 nm |
| `rn-5a374ade1b89` | 3.2 nm vs 2.2 nm |
| `rn-35192771d472` | 2.4 nm vs 1.2 nm |
| `rn-41e5061725f4` | 4.4 nm vs 2.5 nm |

### `hamburg-hadag` (7 TRIM items)

**Receipt:** `PROPOSAL-FIDELITY-hamburg-hadag.md`

| Route ID | Card vs route delta |
|----------|---------------------|
| `rn-24451443bb54` | 3.0 nm vs 1.5 nm |
| `rn-3aec7fb1f836` | 2.0 nm vs 0.8 nm |
| `rn-a5add4c4928b` | 1.0 nm vs 1.5 nm |
| `rn-9771964f7bdc` | 1.0 nm vs 0.6 nm |

### Acceptance

Both → `PASS`, `trim=0`.

---

## PR-4 — Fidelity TRIM: remaining PASS_WITH_FLAGS (5)

| Partner | TRIM count | Receipt | Route IDs to sync |
|---------|------------|---------|-------------------|
| `nyc-ferry` | 4 | `PROPOSAL-FIDELITY-nyc-ferry.md` | `rn-b2490e3f6350`, `rn-711092a81931` |
| `sf-bay-ferry` | 4 | `PROPOSAL-FIDELITY-sf-bay-ferry.md` | `rn-cabe543d04e9`, `rn-a82989283656`, `rn-1ffa4b3d5058` |
| `transport-nsw` | 3 | `PROPOSAL-FIDELITY-transport-nsw.md` | `rn-0d609ac0ab33`, `rn-2b603df666b7` |
| `boston-mbta-ferry` | 2 | `PROPOSAL-FIDELITY-boston-mbta-ferry.md` | `rn-a0edcc795e58`, `rn-0183727f495b` |
| `stockholm-waxholm` | 1 | `PROPOSAL-FIDELITY-stockholm-waxholm.md` | `rn-72110604025d` |

**Pattern:** update `journeys_unlocked[].distance_nm` and matching `phases[].featured_routes[].distance_nm` to sealed route length. Do not change `route_id` bindings.

---

## PR-5 — P2 narrative copy pass

From `GROK-TASKLET-PARTNER-COVERAGE-HANDOFF.md` §P2. Polish only — **no structural edits**, no `route_id` changes.

| Partner | Priority fields |
|---------|-----------------|
| `qatar` | `hero`, `why_now`, `phases[].narrative`, `growth_case.public_value.levers` |
| `hong-kong` | same |
| `transport-nsw` | same |
| `thames-clippers` | same + clarify Uber Boat / authority relationship |
| `nyc-ferry` | same |
| `bc-ferries` | full rewrite toward PTA convention (overlaps PR-6) |
| `wsf` | same |
| `hawaii` | same + Quanta-LR roadmap handling per handoff |
| `bahrain-motc` | `phases[0]` stub narrative only — KPI/regulator prose; geometry sealed |

### Narrative checklist (per partner)

- [ ] `partner_context.their_ambition` — cites authority's **published** network / policy target
- [ ] `partner_context.their_pressure` — congestion, emissions, access (not commercial TAM)
- [ ] `hero.title` / `hero.subtitle` — authority-facing, not marketplace
- [ ] `why_now` — ties to decarbonization mandate + existing water service
- [ ] `phases[].label` + `phases[].scope` — geography-led (Starter → Full network → Mature), not Prove/Scale/Mature
- [ ] `objections[]` — subsidy / fare integration answered credibly
- [ ] No SOM/SAM/TAM/GMV/journey-wallet language anywhere in partner JSON

---

## PR-6 / PR-7 — PTA Batch 6 (6 partners outside the lane)

These have `archetype: public_transit` but **no** dossier, seal receipt, or PTA economics.

| `partner_id` | Display | Current state | Suggested batch |
|--------------|---------|---------------|-----------------|
| `bc-ferries` | BC Ferries | Commercial `growth_case`; 0 sealed corridors in fidelity | **PR-6** (with `hawaii`) |
| `hawaii` | Hawaiʻi — Pulama Lānaʻi × DOT Harbors | Commercial ladder; Quanta-LR roadmap notes in handoff | **PR-6** |
| `wsf` | Washington State Ferries | In batch-5-style handoff for copy pass; not in PTA pair table | **PR-7** |
| `fullers360` | Fullers360 | Commercial ladder; Auckland commuter ferry | PR-7 |
| `maldives-government` | Maldives — National Ferry Network | Commercial ladder | PR-7 |
| `norway-fjords` | Fjord Norway | Commercial ladder; do not full-relink after repair | PR-7 |
| `shun-tak` | Shun Tak / TurboJET | Commercial ladder; HK/Macau cross-harbour | PR-7 |

### Per-partner deliverables (Bahrain gold template)

For each new authority, Tasklet ships **one PR** containing:

#### 1. Dossier — `handoff/partner-map-model/PTA-DOSSIER-<slug>.json`

Schema from `PTA-DOSSIER-bahrain-motc.json`:

- `authority` — display, jurisdiction, existing operator
- `policy_targets` — net-zero / decarb year, `relevance` prose
- `interpretation_for_navier` — full paragraph (no truncation)
- `domestic_network.boarding_points[]` — candidate anchors `[lng,lat]`, labels
- `domestic_network.domestic_pairs[]` — pair IDs, hub/spoke
- `regional_links[]` — optional cross-border (amber roadmap tier)
- `routing_hazards[]` — land/causeway/shallow traps for Grok waypointing

#### 2. Partner rewrite — `data-clean/partners/<slug>.json` + mirror

| Block | Requirement |
|-------|-------------|
| `archetype` | `"public_transit"` |
| `_public_transit_authority` | `archetype_id`, `home_cities[]`, `phase_tiers[]` |
| `partner_context` | Authority ambition / pressure / where Navier fits |
| `hero` | Authority proposal eyebrow |
| `phases[]` | Domestic-first; `featured_routes[]` with `route_id: null`, `_link_status: "pending-seal"` until Grok seals |
| `journeys_unlocked[]` | Same pending-seal pattern |
| `growth_case` | PTA convention labels; `public_value` block with qualitative levers; **no** commercial ladder rungs |
| `growth_case.modal_headline` | `"What it returns to the public"` |
| `growth_case.modal_lead` | Public-value framing |

#### 3. Grok routing spec — `handoff/partner-map-model/GROK-SPEC-<slug>-domestic-routing-YYYY-MM-DD.md`

Copy structure from `GROK-SPEC-bahrain-motc-domestic-routing-2026-06-30.md`:

- §1 What Tasklet has done
- §2 What Grok owns (mint BPs, hand-waypoint, land QA, bind route_ids)
- §3 Hazard table (authority-specific)
- §4 Acceptance gate checklist
- §5 Economics regen pointer (`PTA-ECONOMICS-CONVENTION.md`)

#### 4. Gap table row — append to `PTA-PAIR-GAP-TABLE.json` + regenerate `.md`

#### 5. Handoff receipt in PR description

```
Tasklet PTA Batch 6 — <display>
- Dossier: handoff/partner-map-model/PTA-DOSSIER-<slug>.json
- Partner: data-clean/partners/<slug>.json (synced to partner-pitch/)
- Grok spec: handoff/partner-map-model/GROK-SPEC-<slug>-domestic-routing-*.md
- Seal targets: N domestic + M regional
- Fidelity pre-seal: PASS (pending-seal nulls OK)
```

**Grok follows** with seal script + `regen_pta_economics.py --partner <slug> --apply`.

### Batch 6 notes

- **`bc-ferries`:** Island Class electrification narrative; Gulf Islands + Tsawwassen–Swartz Bay pressure. Existing `partner_context` is a good seed — replace commercial `growth_case` entirely.
- **`hawaii`:** Inter-island vs harbour commute framing; Quanta-LR on roadmap legs only (`render: amber-dashed`). See `GROK-HANDBACK` §P3 Hawaii growth_case.
- **`norway-fjords`:** After Tasklet rewrite, Grok runs repair + promote only — **no full relink** (`GROK-TASKLET-PARTNER-COVERAGE-HANDOFF.md` do-not-touch).
- **`shun-tak`:** Cross-harbour + airport ferries; clarify public-authority vs commercial operator relationship in narrative.

---

## Quick reference — 16 clean PASS (presentation only)

No fidelity TRIM work. PR-1 scrub + optional PR-5 narrative polish:

`abu-dhabi-itc`, `auckland-ferries`, `bangkok-chao-phraya`, `dubai-rta`, `hong-kong`, `istanbul-sehir-hatlari`, `kochi-water-metro`, `lisbon-transtejo`, `qatar`, `rakta`, `singapore-mpa`, `thames-clippers`, `vancouver-seabus`, `venice-actv`, `wsf`, `bahrain-motc`

---

## Grok idle / watching

- Will regen economics after Batch 6 dossiers + seals land
- Will rebuild + deploy after Tasklet PRs merge to `main`
- Do not re-run full `relink_hub_market_featured.py` on `norway-fjords`

---

*Grok seat · navier-atlas · Tasklet intake checklist · refs `a0ab6dfc`*