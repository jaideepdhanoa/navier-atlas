# Minor Hotels — Grok Seal Package (2026-06-22)

**Status: `research-complete / seal-needed`** — an INPUT package for Grok's deterministic seal lane.
NOT `proposal-complete`. GitHub `main` is source of truth; this zip is an input, not a hand-back.

## What this is
Minor Hotels is a **new archetype**: a hotel developer/operator whose marine network is a **captive
intra-portfolio graph** (routes only from/between its own hotels + curated excursions), sized by guest
throughput at ~0.85–0.90 capture — NOT a contested ride-hail mobility share.

> **Deck archetype (reusable):** build Minor's deck with the operator-developer variant at
> `deck-studio/docs/OPERATOR-DEVELOPER-ARCHETYPE.md` (captive property-graph routes, captive economics,
> WIDTH headroom) — also mirrored in the workspace `partner-deck-grok-handoff` skill; reuse for Aman /
> Four Seasons / Constance going forward.

## Read order
1. `docs/GROK-SEAL-PROMPT.md` — the mandate, deterministic field mappings, eval gates G1–G8, cascade order.
2. `docs/01-MINOR-ARCHETYPE-GUIDE.md` — how a hotel developer differs (route rule + captive economics).
3. `docs/02-MINOR-NARRATIVE-AND-USP.md` — thesis, four USP value props, proof strip (Tasklet-owned narrative).
4. `docs/03-MINOR-CLUSTER-SUBPROPOSAL-PLAN.md` — 13 cluster sub-pages, grounded-first build order.

## Contents
- `inputs/binds/` — 64 exact binds (3 cluster files) + Tweed/Casuarina attach + 9 pipeline future-BP binds.
- `inputs/seeds/` — Gulf-of-Thailand, Algarve+Porto grounding, P1 (Sunshine Coast + Mozambique),
  P2/P3 (Vietnam + AU tail), P3 Euro/Brazil singletons, registry expansion queue.
- `inputs/economics/` — 3 grounded flagship floors: Phuket $4.38M, Palm $3.75M, Bali $0.63M.
- `inputs/inventory/` — full 221-property inventory.
- `inputs/minor-hotels-COVERAGE-AUDIT.json` — 0-silent-drops reconciliation (109 coastal → bound/seeded/
  attach/pipeline/held).
- `inputs/seal-manifest.json` — file index + coverage/economics/grounding/held summary.

## Confirmed directives baked in
- Captive `hospitality_developer` archetype; routes only within Minor's property graph (G1 archetype purity).
- LB-254 captive capture: TAM anchors on transport_spend_pool, never floor/0.10; headroom = WIDTH.
- Palm Jumeirah crescent needs BP grounding before its floor cascades (G8).
- 2 single-property markets HELD (Villa Padierna Marbella, AfroChic Diani) — null beats confidently-wrong.
- Exactness over coverage; ID-based matching; null beats confidently-wrong; broad-footprint-first,
  exact-bind-second. External outreach stays draft; this is an internal handoff.
