# GROK-SPEC — Brazil premium-substitute fare cascade ($28) + Sheet rebuild

**Date:** 2026-07-15
**Owner:** Tasklet (source + artifacts) → Grok (deterministic Sheet rebuild + model-to-Sheet parity)
**Scope:** DiDi Brazil and inDrive Brazil only. No other market's economics change.

## Decision (Jaideep, explicit)
Brazil Rio cross-bay corridors are benchmarked to a **premium-tier substitute** (Uber Black-equivalent), not basic/commuter fares.
Confirmed comparable fare = **$28 / one-way seat**.

Fare sourcing: UberX Rio↔Niterói average R$63–73; Uber Black ≈ 2.1× ≈ R$143; BRL/USD 5.07 → ≈ **$28**.

## Mechanism (do NOT change the global floor)
- Per-corridor override field `comparable_fare_usd_pax = 28.0` applied to the **four** Rio Brazil corridors only (LB-255 mechanism).
- Commuter `_fare_record` retained on each corridor as provenance; a premium `_fare_record` note added.
- Global `premium_ondemand_floor = 18.0` in `vessel-constants.json` is **unchanged** (touching it would move Yassir, Swing, Kakao, Marti, etc. — wrong blast radius).

## Source files changed in this PR
- `finance/model/corridors.json` — 4 Rio Brazil corridors: `comparable_fare_usd_pax = 28.0`.
- `finance/recal/corridors-indrive.json` — inDrive scoped view mirrors the same 4 corridors at `28.0` (finance corridor-inheritance parity; Egypt corridors untouched at $32 / $50).

## Regenerated artifacts in this PR (deterministic)
- `finance/recal/agg-didi.json` — regenerated via `aggregate.py --partner didi`.
- `finance/recal/agg-indrive.json` — regenerated via `aggregate.py --partner indrive --corridors corridors-indrive.json`.
- `deck-studio/decks/gen_deck_economics.py` — **unit-economics slide now reads the `mid` scenario block** (see note below).
- `deck-studio/decks/didi-brazil/economics-binding.json` — TAM floor/pool + `economics_routes` (4) added.
- `deck-studio/decks/didi-brazil/generated-deck-economics.json` — regenerated via `gen_deck_economics.py`.
- `deck-studio/decks/indrive-brazil/economics-binding.json` — same, mirrored.
- `deck-studio/decks/indrive-brazil/generated-deck-economics.json` — regenerated.

## Scenario basis: mid (matches the TAM slide)
Per Jaideep: the deck is centered on the **mid** scenario. The country/TAM rollup already read `mid`; the per-city unit-economics slide now reads `mid` too (previously read the conservative `thin` block). This puts the unit-economics slide and the TAM slide on one consistent scenario. Applies to all partner decks generated by `gen_deck_economics.py`.

## Deterministic outputs for verification (mid basis)
N30 8-seat vessel, 55% revenue-leg, 274 operating days, mid load 0.55.

| Corridor | route_id | nm | fare | rev/boat/yr | margin | payback |
|---|---|---|---|---|---|---|
| Praça XV → Arariboia | rn-1886629dbf0c | 2.7 | $28 | $329,190 | 75.9% | 2.40 yr |
| Praça XV → Charitas | rn-80f0d0ebe0bd | 4.4 | $28 | $329,190 | 75.7% | 2.41 yr |
| Praça XV → Cocotá | rn-369ef0eb69d9 | 6.0 | $28 | $329,190 | 75.5% | 2.41 yr |
| Praça XV → Paquetá | rn-00bb6ded4be5 | 9.2 | $28 | $263,278 | 69.2% | 3.29 yr |

Country grounded floor: **113 vessels**, **$36,407,526 / yr** annual route revenue; addressable Brazil water-cross pool **$367,461,220**.

TAM ladder (both decks): Rung 1 (SOM, supported) $36.41M → Rung 2 (SAM, addressable water-cross) $367.46M → Rung 3 held null (not sourced).

DiDi Brazil and inDrive Brazil are **identical** on all of the above (shared canonical Brazil basis). Any future Brazil-reference revision must cascade to both.

## Grok actions
1. Rebuild the DiDi Brazil and inDrive Brazil economics Google Sheets **in place** from the regenerated `agg-*.json` / `generated-deck-economics.json` (Slides/PPTX not touched here; deck rebuild handled separately on the Voi-duplicate machine).
2. Establish and confirm **model → Sheet parity** for both decks before any deck clearance.
3. Preserve the 4-corridor structure (one economics row per Rio corridor).

## Non-economic metadata note (expected, no action needed)
Regenerating `agg-didi.json` also refreshes annotation-only fields in the `assumptions` block for Costa Rica and Mexico rows (`charge_recovery_min`, `charge_recovery_method`, `charge_recovery_engineering_status`, `gross_legs_per_day`, reworded `trips_per_day_derivation`) to match the current `atom.py`. **Every economic value for those markets is byte-identical** — fare, pax, revenue, margin, payback, and rollups unchanged. This is the committed aggregate catching up to current code, not an economics change.
