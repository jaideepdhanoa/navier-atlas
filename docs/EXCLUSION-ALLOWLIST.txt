# Exclusion-token ALLOWLIST — exact phrases permitted despite containing a blocked token.
# Each line is a literal phrase (case-insensitive). Before the §3.2 leak grep (and the build-site
# per-partner sweep) run, these phrases are neutralized in the scanned text — so the blocked token is
# still caught EVERYWHERE ELSE, just not inside one of these vetted, partner-facing contexts.
# Keep this list tiny and specific; prefer a Tasklet copy reword when feasible.
#
# 2026-05-31: "exclusivity" is a blocked token (meant to catch commercial/deal-exclusivity promises).
# Red Sea Global's guest FAQ uses it in the hospitality sense (guest seclusion), which is fine to ship.
guest privacy and exclusivity
#
# 2026-06-01: Bora Bora (French Polynesia) brief describes the LUXURY-TOURISM market — "a visitor economy
# built on exclusivity" — public-facing tourism copy, not a deal-exclusivity promise. Tasklet asked to
# reword at source (brief is sealed, so Claude can't); drop this line once they do.
visitor economy built on exclusivity
#
# 2026-06-01 (partners+cities drop): three more luxury-tourism uses of "exclusive/exclusivity" in
# partner-facing copy (not deal-exclusivity promises). Same treatment as above — vetted-safe; Tasklet
# may reword at source later.
exclusive low-density luxury islands
exclusivity of a helicopter
exclusive summer harbours
#
# 2026-06-01 (partners+cities drop): the Zanzibar city brief has a demand-signal stat whose VALUE is the
# internal taxonomy label "Archetype fit" (data slip, not a metric). Not sensitive, so allowlisted to
# unblock the deploy — but it ships as weak public copy. Tasklet asked to reword the stat at source;
# DROP THIS LINE once they do (see NOTES-FOR-TASKLET 2026-06-01).
Archetype fit
