# Live state — backlog, PRs, workflows (snapshot 2026-06-25)

This is the handover state. Verify each PR's merge/live status on `main` before treating it as done
(several were pushed but not yet merge-verified).

## Open PRs to verify / land
- **#106** — Hospitality gold: Minor econ realism propagated to the **Minor-scoped** layer (override file +
  default-off engine hook), gold economics source, and `HOSPITALITY-DECK-GOLD.md` build spec. *Next:* review
  + land; then run the Minor model cascade so the sheet/tracker honor the override (the live deck stays as-is).
- **#105** — Bolt corridor pins + BP research + parked follow-up. *Next:* confirm merge/live + build ingest.
- **#103 / #102** — sub-proposal route geometry live; partner-facing copy/proof pages still need plain-English
  follow-through.
- **#101** — de-jargon pass; Grab deck KPI refresh still queued behind it.
- **#99** — city-brief completion sweep (confirm 145/145 validator state on `main`; no stale combined briefs).
- **#98** — Ocean Whisperer prose cleanup + ABC briefs (confirm live).
- **#95** — Caribbean consolidation / ABC-islands binding (confirm bindings attach to `abc-islands`; reconcile
  economics). See `abc-curacao-2026-06-24/CARIBBEAN-CONSOLIDATION.md`, `partner-pitch/partners/caribbean.json`.

## In flight this session (Tasklet → Grok chat)
- **Minor Hotels v2 deck** — gold, verified live (24-slide, 7 gold econ slides). Do **not** ask build to
  rebuild it; corrected data lives in source. Live deck:
  `docs.google.com/presentation/d/1p5NtoaORRWyBpcsbfqnSB9PLg9yyTpvuzJAyBMjen4o/edit`.
- **LINE MAN Wongnai × Navier** — new partner proposal authored as a narrative-only mirror of Grab Thailand
  (`partner-pitch/partners/line-man-wongnai-derivative.json`) + a deterministic deck package
  (`deck-studio/decks/line-man-wongnai/`: config, `SLIDE-COPY-DIFF.md`, banked cover logo, Grok create
  prompt). *Decision baked in:* Thailand-scoped (drops Grab's 8 regional PH/MY/TW slides). *Next (build):*
  create the live deck from the copy-diff; *Next (you):* re-run the Thailand cascade under
  `partner_id=line-man-wongnai` to mint its own `economics_url` (until then it reuses Grab Thailand numbers).

## Backlog (from Tasklet)
- **OW capture basis** — guidance says 55% captive-style (not FP-flat); decide whether to cascade
  (`PR #101`, `GROK-SPEC-ow-greenfield-captive-rescope.md`).
- **Growth-ladder cascade for 36 no-ladder proposals** — cascade TAM ladders on minted `route_id`s
  (`handoff/grok-handback-2026-06-24.md`).
- **Bolt data-bug A** — sub-$10M one-decimal money formatting in the renderer (`$1.54M` must not round to `$2M`).
- **Bolt Bug C / Issue #104** — labelled template-band re-cascade + reseal (handed to build; await execution).
- **SEAL re-seal package** — advisory; reseal at the next gold tag after corridor pins settle.
- **Grab deck KPI refresh** — post-#101 de-jargon follow-up.
- **Grab Thailand outreach reply** — CEO-level draft exists, not sent (external = draft; await Jaideep).

## Active workflows (autonomous)
- **Daily backstop leak sweep** — posts to `#tasklet-jaideep` when a jargon leak is detected.
- **Qatar deck comment watch** (fast Drive push + email backup) — apply JSON → renderer → Drive, post to
  `#tasklet-google-slide-comments`, archive.
- **Gmail Drive comment notification watch** — read email, apply edit, post, archive.
- **Hourly deck QA debounce sweep** — currently disabled.

## Channels / surfaces
- `#tasklet-jaideep` — finance/gold/seal receipts + numbers.
- `#tasklet-google-slide-comments` — deck-comment workflow output.
